/**
 @usage

 ##### Initialization
 ```javascript
 const calculator = new Calculator();
 ```
 */
export class Calculator {
  /**
   * @description Adds provided `numbers`.
   * @example
   * ```javascript
   * const result = calculator.add(1, 2, 3);
   * ```
   */
  // @ts-ignore
  public add(...numbers: number[]): number {
    return 0;
  }

  /**
   * @description Subtracts provided `numbers`.
   *
   * @example
   * ```javascript
   * const result = calculator.subtract(1, 2, 3);
   * ```
   */
  // @ts-ignore
  public subtract(...numbers: number[]): number {
    return 0;
  }
}
